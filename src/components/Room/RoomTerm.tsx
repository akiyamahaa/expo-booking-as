import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const RoomTerm = (props: Props) => {
  return (
    <View>
      <Text>RoomTerm</Text>
    </View>
  )
}

export default RoomTerm

const styles = StyleSheet.create({})